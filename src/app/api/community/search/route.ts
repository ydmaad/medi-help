import { supabase } from "@/utils/supabase/client";
import { NextRequest } from "next/server";

export async function GET() {
  //   const { data, error } = await supabase.from("posts").eq("id", id);
}
